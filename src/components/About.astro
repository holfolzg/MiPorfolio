<section class="about" id="about">
  <div class="about-container">

    <!-- IMAGEN -->
    <div class="about-image">
      <div class="image-wrapper">
        <img src="/public/miFoto.png" alt="Foto de Jhoan" />
      </div>
    </div>

    <!-- TEXTO -->
    <div class="about-content">
      <p class="about-label">Sobre mí</p>
      <h2 class="about-title">Desarrollador Frontend</h2>
      <p class="about-description">
        Desarrollador web enfocado en frontend, especializado en la creación de
        interfaces modernas, rápidas y escalables. Trabajo principalmente con
        JavaScript, TypeScript, Astro y Angular para construir experiencias web
        optimizadas y de alto rendimiento.
      </p>
    </div>

  </div>

  <style>
    .about {
      position: relative;
      padding: var(--section-gap) 1.5rem;
      overflow: hidden;
    }

    .about::before {
      content: '';
      position: absolute;
      bottom: -10%;
      right: 0;
      width: 500px;
      height: 300px;
      background: radial-gradient(ellipse, var(--color-purple-glow), transparent 70%);
      z-index: 0;
    }

    .about-container {
      position: relative;
      z-index: 1;
      width: 100%;
      max-width: var(--max-width);
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    /* IMAGEN */
    .about-image {
      display: flex;
      justify-content: center;
    }

    .image-wrapper {
      width: 320px;
      height: 320px;
      border-radius: 50%;
      overflow: hidden;
      border: 1px solid var(--color-border);
      background: var(--color-surface);
      box-shadow: 0 0 40px var(--color-purple-glow);
    }

    .image-wrapper img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* TEXTO */
    .about-label {
      font-family: var(--font-mono);
      font-size: 0.8rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--color-accent);
      margin-bottom: 0.75rem;
    }

    .about-title {
      font-size: clamp(1.8rem, 4vw, 2.8rem);
      margin-bottom: 1.25rem;
    }

    .about-description {
      font-size: 1.05rem;
      line-height: 1.75;
      max-width: 480px;
    }

    /* ─── RESPONSIVE ─── */

    @media (max-width: 900px) {
      .about-container {
        grid-template-columns: 1fr;
        text-align: center;
      }

      .about-image {
        margin-bottom: 1rem;
      }

      .image-wrapper {
        width: 220px;
        height: 220px;
      }

      .about-description {
        margin: 0 auto;
      }
    }

    @media (max-width: 768px) {
      .about {
        padding: 4rem 1.2rem;
      }
    }
  </style>
</section>